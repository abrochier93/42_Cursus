****** LISTE DES TESTS A FAIRE *******

1. TEST DES SIGNAUX
	ctrl-C affiche un nouveau prompt sur une nouvelle ligne même si prompt avec texte.
	ctrl-D quitte le shell et écrit "exit" dans la sortie standard; sauf si prompt avec texte
	ctrl-\ ne fait rien : avec un prompt vide ou avec un prompt avec du texte

2. TEST DES BUILTINS
	~ echo salut ca va : renvoie salut ca va
	~ echo "salut ca va" : renvoie salut ca va
	~ echo "salut ca va : renvoie Error
	~ echo 'salut ca va : renvoie Error
	~ echo "'salut ca va"' : renvoie Error
	~ echo '"salut ca va'" : renvoie Error
	~ echo '$PATH' : renvoie $PATH
	~ echo $PATH : renvoie la valeur de PATH
	~ echo $VAR_INEXISTANTE : renvoie une ligne vide
	~ echo "$PATH" : renvoie la valeur de PATH
	~ echo '"$PATH"' : renvoie "$PATH"
	~ echo "'$PATH'" : renvoie ' + la valeur de PATH + '
	~ echo "'$PATH" : renvoie ' + la valeur de PATH
	~ echo '$PATH "' : renvoie $PATH  + "
	~ echo "$PATH '$PATH'" : renvoie la valeur de $PATH + espace + ' + la valeur de $PATH + '
	~ echo "' $VAR_INEXISTANTE '" : renvoie '  ' (fait comme si $VAR_INEXISTANTE  n'est pas lu)
	En conclusion : 

3. TEST HISTORIQUE

